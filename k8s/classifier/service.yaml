apiVersion: v1
kind: Service
metadata:
  name: classifier
  labels:
    app: classifier
    app.kubernetes.io/name: classifier
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: microproyecto2-nube
spec:
  type: LoadBalancer
  selector:
    app: classifier
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
